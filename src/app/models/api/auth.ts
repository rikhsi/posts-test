export interface Signin {
    name: string | null;
    username: string | null;
    email: string | null;
    password: string | null;
}

export interface Login {
    username: string | null;
    password: string | null;
}