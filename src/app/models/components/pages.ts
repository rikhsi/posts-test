export enum BasicPages {
    auth = 'auth',
    admin = 'admin'
}

export enum AuthPages {
    login = 'login',
    register = 'register'
}

export enum AdminPages {
    list = 'list',
    post = 'post/:id'
}